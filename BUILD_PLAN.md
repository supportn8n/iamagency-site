> ✅ АКТИВНО (2026-06-20): ночной цикл ВКЛючён — фиксить ошибки homepage + достроить Маркетинг, проверять рендер всю ночь.

## ⭐ Метод сборки блоков = Builder.io (Figma→HTML), НЕ перерисовка
- Пользователь экспортирует фрейм через плагин Builder.io → даёт URL вида `https://api.builder.io/api/v1/file/assets/TEMP/<id>`.
- Конвейер: `builder/build_block.py` (добавить (slug, assetId, RU) в BLOCKS, запустить) → качает HTML+картинки в `public/blk/<slug>/`, переводит в `src/app/blocks/gen/<slug>Html.ts`.
- Рендер: `<BuilderBlock html={<slug>Html} />` (нативные px 1440×1024, авто-масштаб). Импорт+вставить в `src/app/page.tsx` по порядку.

## 🐞 ОШИБКИ HOMEPAGE (от пользователя 2026-06-20) — чинить
1. ✅ (сделано) Hero был уже нижних блоков (fit-mode) → зона Hero теперь `w-full`, одинаковый размер. [если юзер хочет — пересобрать Hero из его Builder.io-экспорта 0qdc1468tzcmqliwj6h]
2. ✅ ЗАКРЫТО. Текст наезжал — причина: подмена Coolvetica→Oswald + однострочные тексты переносились. ФИКС: (а) скачал Coolvetica с кириллицей (Яндекс.Диск → public/fonts/coolvetica.otf, @font-face в globals.css), (б) build_block.py: убрал подмену на Oswald + добавил _nowrap (однострочным элементам height<font*1.5 → white-space:nowrap). Услуги — в одну строку, проверено.
3. ⬜ «Каменная фигура умерла» (белый 3D-объект) — ВЕРОЯТНО auto-fix т.к. весь homepage теперь из Builder.io (реальные картинки, не плейсхолдер white1). ПРОВЕРИТЬ скрином Услуги/где была фигура; если битая — глянуть /blk/<slug>/.
4. ✅ ЗАКРЫТО (тот же шрифт Coolvetica). ТАРИФЫ теперь правильным шрифтом.
5. ⬜ «Направления»: кружки с квадратами + текст — собрать блок Направления (часть Маркетинга, экспорт j674u45e88dmqliwj6j). nowrap-фикс должен помочь с текстом.
- ✅ Шрифт Coolvetica (с кириллицей) скачан и подключён. Файл: public/fonts/coolvetica.otf (+ italic).

## 🧱 ЭТАП: Маркетинг (новые экспорты Builder.io, файл vcp-a0a1a3f5)
Маркетинг = большая секция из частей (собрать через build_block.py, добавить в page.tsx по порядку):
- Маркетинг шапка `xd7ljycrdemqlguscc` | Направления `j674u45e88dmqliwj6j` (сетка направлений, есть «+»-кружки, эмодзи=мем) | Кейсы `apeb02a2lkdmqliwj6m`,`bnw1qrytneemqliwj6p` (инфинити-скролл карточек) | Создание `s7nsthpckxmqliwj6s` | Q&A `7bban6jd39pmqliwj6u`,`x4cbzbippznmqliwj6v` | Процесс `woxje2u098fmqliwj6x` (h=780) | Скидка `b7nkaihbjgemqliwj6z` (h=780, обратный отсчёт, дирижабль/«золотая гора») | Кейсы 3 `aodylbml8ygmqliwj70`(h595) | Кейсы 2 `57ajbri6qajmqliwj71`(h595) | Group-строки инфинити-скролла 1311×180 (s1svq95gfomqliwj73 и Group 1437253853-856) | Group-карточки 254×192 (1437253842-852) | мелкие 211×117 (1437253829-833).
- АНИМАЦИИ (best-effort): «мем» + «золотая гора». Попробовать GIF через `mcp__figma__download_figma_images` (gifRef) — но `get_figma_data` падает на кириллице, gifRef достать тяжело. Fallback: CSS-анимация (появление/парение). Инфинити-скролл — паттерн как `Partners.tsx` + `.marquee-track`.
- ВНИМАНИЕ: высоты блоков РАЗНЫЕ (1024/780/595/...). BuilderBlock сейчас хардкодит 1440×1024 → сделать проп `h` (высота фрейма) для правильного aspect-ratio.

## 🌙 Ночной цикл (каждые ~20 мин)
1. Скрин homepage через browse (бинарь /c/Users/gromo/.claude/skills/gstack/browse/dist/browse, несколько вьюпортов), сверить с дизайном/скринами ошибок.
2. Взять следующий ⬜ из «ОШИБКИ» или следующий несобранный кусок Маркетинга → сделать ОДНО → deploy `npx vercel --yes --prod` → скрин-проверка → отметить тут.
3. Шрифт Coolvetica/фигуры — если нет ассета, пометить «жду юзера», делать что можно.
4. ScheduleWakeup ~1200с тем же промптом, пока не пройдёт по всем ошибкам + Маркетингу.

# Автономная сборка home page iamagency — план + состояние

## ⭐ SVG-ЭКСПОРТ ВСЕГО ДИЗАЙНА (главный источник, дал юзер 2026-06-19)
Папка `C:\Users\gromo\Downloads\IAM WEB\` — SVG каждого блока: Hero.svg, Партнеры.svg, УТП.svg, Услуги.svg, Тарифы.svg, Портфолио.svg, Маркетинг.svg, Отзывы.svg, Школа.svg, Блог.svg, Контакты.svg, Футер.svg + Group*.svg. ЭТО ЛУЧШИЙ ИСТОЧНИК — точные позиции/текст/цвета + встроенные растровые фигуры (base64). Для новых блоков парсить SVG (координаты viewBox, embedded images, текст), а не выковыривать через браузер/API. Для блоков с 3D-фигурами — вытаскивать base64-картинки из SVG.

## Метод
- Figma API заблокирован (лимит на АККАУНТ, не на токен — проверено двумя токенами). Все ассеты тяну через браузер: CDP к Chrome 9222 (`tools/cdp.py`, `tools/open_tab.py`), `websocket-client` + `suppress_origin=True`, запуск с `dangerouslyDisableSandbox`.
- Делаю САМ, по шагам. Под-агенты не использую (зависают).
- Деплой: `npx vercel --yes --prod`. Прод: https://iamagency-site.vercel.app
- Каждую итерацию: сделать 1 шаг → задеплоить → скрином проверить → отметить здесь → ScheduleWakeup ~15 мин.

## Условия окружения (если отвалится — встанет)
- Chrome 9222 открыт + залогинен в Figma.
- Эта сессия/терминал открыт.
- Vercel CLI залогинен.

## Принцип (как просил клиент)
Дизайн порезан на блоки только для удобства. Фоновые фигуры (вязка и т.п.) — СКВОЗНЫЕ, идут через несколько блоков. Поэтому:
1. Сначала собрать ВСЮ ПОДЛОЖКУ (фон) целиком, от начала до конца.
2. Потом класть контент блоками поверх подложки, сверху вниз.

## Блоки homepage (сверху вниз, node-id)
| Блок | node-id |
|------|---------|
| Hero | 10001:39 |
| Партнёры | 12001:10 |
| УТП/Цифры | 12003:116 |
| Услуги | 12043:300 |
| Тарифы | 12050:403 |
| Портфолио | 12050:384 |
| Маркетинг | 12096:42 |
| Отзывы | 12096:49 |
| Школа | 12099:154 |
| Блог | 12099:192 |
| Контакты | 12099:195 |
| Футер | 12100:252 |

## Сквозные фоновые фигуры
- Вязаный узел **12019-5** (~798×563) — идёт через Hero → Партнёры → УТП. ЦЕЛЬНАЯ, не резать.
- (выявить остальные: стеклянный/фиолетовый объект справа в Hero, белый объект, и ниже по странице)

## ЗАДАЧИ

### PHASE 0 — понять целое (как соединены блоки/фигуры)
- [ ] 0.1 Снять полный референс страницы: экспортнуть каждый блок-фрейм картинкой, сложить встык в `figma-ref/full/` → понять состыковку.
- [ ] 0.2 Выявить ВСЕ сквозные фоновые фигуры + их позиции/размеры (через Dev Mode «measurements»).

### PHASE 1 — подложка (фон)
- [ ] 1.1 Экспортнуть каждую фоновую фигуру цельным прозрачным PNG (включая правильную вязку 12019-5).
- [ ] 1.2 Собрать страницу полной высоты с фигурами на абсолютных позициях = фоновый слой (`src/app/blocks/Background.tsx`).
- [ ] 1.3 Деплой + проверка скрином.

### PHASE 2 — контент поверх (по блокам сверху вниз)
- [ ] Hero (текст/меню/иконки/кнопка — уже почти есть, пересадить на подложку)
- [ ] Партнёры (логотипы лентой поверх вязки — ассеты logos.png уже есть)
- [ ] УТП/Цифры, Услуги, Тарифы, Портфолио, Маркетинг, Отзывы, Школа, Блог, Контакты, Футер

### Итерация 2 (автоном)
- API всё ещё 429 (лимит на аккаунт держится). Chrome 9222 жив.
- Сверил вязку с эталоном `figma-ref/01_hero.png`: должна быть внизу-слева, крупная, доставать до низа Hero и заходить в Партнёры. Подогнал координаты в page.tsx: вязка `left 2cqw / top 38cqw / width 76cqw`. Задеплоено, проверено скрином: вязка крупная внизу-слева, заходит в Партнёры, логотипы проходят через нижнюю петлю — совпадает с эталоном. ✅ ПОДЛОЖКА (вязка) готова.
- Hero-фигуры white1.png (белый сверху) и purple.png (стекло справа) — на местах, совпадают с эталоном.
- ДАЛЕЕ: проверить, есть ли фоновые фигуры НИЖЕ (в УТП/Услуги/и т.д.); потом контент-блоки сверху вниз (следующий — УТП/Цифры 12003:116, пересобрать как 1:1).

### Итерация 3 (автоном)
- API 429 (держится). Эталон 03_stats.png: 4 карточки 2×2 + кнопка, сверху заходит вязка, справа оранжевое КОЛЬЦО (ещё одна фоновая фигура — добавить позже).
- ✅ Пересобрал блок **УТП/Цифры как 1:1 фрейм**: массив `utp` с координатами P(), карточки 450+ (#141414), 18 (лавандовый градиент), 7 лет (оранжевый градиент), -30% (серый #eceae7) + кнопка «Получить бесплатную консультацию» по центру. Задеплоено, скрин совпадает с эталоном.
- ОСТАЛОСЬ по этому блоку: фоновые фигуры (продолжение вязки сверху, оранжевое кольцо справа).
- ДАЛЕЕ: блок Услуги (node 12043:300, тёмная секция) по figma-ref/04_services.png — пересобрать 1:1.

### Итерация 4 (автоном)
- API 429. ✅ Пересобрал блок **Услуги как 1:1 фрейм** (тёмный #141414, рад. 2.8cqw): заголовок «УСЛУГИ» P(64,44) F(86), 5 контурных пилюль 01–05 (P(64, 178+i*92, 1312, 76)), белый объект снизу-слева (плейсхолдер white1.png), описание + синяя кнопка «Записаться на бесплатную консультацию» снизу-справа. Скрин совпал с figma-ref/04_services.png.
- ПЛЕЙСХОЛДЕР: белый 3D-объект в Услугах = временно hero/white1.png; реальный объект услуг выгрузить из Figma позже.
- ДАЛЕЕ: блок Тарифы (node 12050:403) по figma-ref/05_pricing.png.

## Текущее состояние
- Hero ~90% (светлая тема, заголовок-картинка, 3D-объекты-заглушки, иконки Max исправлены). Будет пересобран на подложку.
- Партнёры: есть logos.png (прозрачные логотипы). Вязка под них — нужна ПРАВИЛЬНАЯ цельная 12019-5 (моя 71 2 была кривым куском, выкинуть).

## Стратегия автономного цикла (каждые ~15 мин)
1. **Проверить, не снялся ли лимит Figma API** (`/files` тестовым запросом). Лимит на аккаунт, со временем сбрасывается. Как только API оживёт — это БЫСТРЫЙ путь: одним запросом тяну всю структуру + позиции всех фигур, экспортирую через `/images`, собираю страницу за минуты. Токен: `<токен в .mcp.json>` (или старый <токен в .mcp.json>...).
2. **Если API ещё в лимите** — двигаю сборку через браузер: выгружаю следующую фигуру/блок. НАДЁЖНО выделять через ЛЕВУЮ панель слоёв (клик по имени слоя), а НЕ кликом по холсту (по холсту ненадёжно — фигуры с дырками/наложением). Помощник `tools/export_node.py` (selection по холсту) ненадёжен для фигур — дорабатывать на panel-select.
3. Деплой `npx vercel --yes --prod` → скрин через browse → отметить шаг в плане.
4. ScheduleWakeup ~900с с тем же промптом, пока home page не собран целиком.

## Лог итераций
- (старт) План создан. Новый токен проверен — **лимит на аккаунт** (оба токена → 429 на /files,/images), идём через браузер.
- Попытка экспортнуть вязку 12019-5 через export_node (canvas-select): выделение сработало, но триггер Export пустой — canvas-клик выбрал не ту фигуру/состояние панели.
- ВАЖНО: узел **12019-5 = слой «71 1» = тот же большой вязаный узел = yarn.png** (969×462@1x, у меня уже есть в public/hero/yarn.png @2x). Вязка — ОДНА цельная фигура. Экспорт В DEV MODE РАБОТАЕТ (триггер появляется), выходит без апскейла (1x) если 2x-сеттер не сработал.
- ✅ СДЕЛАНО: вынес вязку в СКВОЗНОЙ фоновый слой (page.tsx, зона Hero+Партнёры, z-1, не обрезается), Hero фон прозрачный, Партнёры = только логотипы (logos.png) z-2 поверх вязки. Задеплоено, проверено скрином: вязка идёт из Hero в Партнёры одной фигурой, логотипы проходят через неё. ОСТАЛОСЬ подогнать позицию/размер вязки точнее под макет (сейчас левее/мельче, в макете крупнее и центральнее) + добавить остальные фоновые фигуры (стеклянный объект справа, белый объект).

## Лог (2026-06-20, вечер)
- ✅ Ошибка 1 закрыта двумя путями: (а) зона Hero → w-full; (б) ВЕСЬ homepage пересобран из Builder.io (build_block.py обновлён на полный набор assetId vcp-3ad73083: hero/utp/uslugi/tarify/portfolio/marketing/otzyvy/shkola/blog/kontakty/futer). page.tsx = чистый стек `<BuilderBlock>` + `<Partners/>` (лента) между Hero и УТП. Все блоки одного размера. Задеплоено, проверено.
- Hero/УТП теперь Builder.io (мой кастомный Hero с вязкой/иконками заменён). Минусы: вязка снова per-block (не сквозная), nav/соц — статичные. Если юзер захочет — вернуть.
- ОСТАЛОСЬ: шрифт Coolvetica (ош.2,4), фигуры (ош.3), Направления (ош.5), достроить Маркетинг + анимации (мем/золотая гора/инфинити). Нужен файл Coolvetica от юзера.

## Лог (ночь 2026-06-20, ~02:40)
- ✅ Шрифт Coolvetica (кириллица) скачан с Яндекс.Диска сам, otf → public/fonts/, @font-face добавлен.
- ✅ build_block.py: убрана подмена Oswald, добавлен _nowrap (однострочные не переносятся). Все 11 блоков перегенерены.
- ✅ Ошибки 1,2,4 закрыты. Полный homepage из Builder.io, единый размер, правильный шрифт, Услуги без наезда — проверено скрином.
- ДАЛЕЕ (ночной цикл): (3) проверить белую фигуру в Услугах/др. (должна быть ок из Builder.io); (5) собрать Направления; достроить Маркетинг (Направления→Кейсы→Создание→Q&A→Процесс→Скидка) + инфинити-скролл + анимации мем/золотая гора. BuilderBlock — добавить проп высоты для блоков ≠1024.

## Лог (ночь ~03:00)
- ✅ Собран блок **Направления** (napravleniya j674u45e88dmqliwj6j) — сетка направлений с «+»-кружками, мем (КЛАСС) справа сверху. Кружки чистые, текст в строку (nowrap), Coolvetica. Ошибка 5 ЗАКРЫТА. Вставлен после marketing в page.tsx.
- BuilderBlock получил проп `h` (высота) для блоков ≠1024 (Процесс/Скидка 780, Кейсы 595).
- ДАЛЕЕ: следующий кусок Маркетинга — Кейсы (apeb02a2lkdmqliwj6m), потом Создание (s7nsthpckxmqliwj6s), Q&A (7bban6jd39pmqliwj6u), Процесс (woxje2u098fmqliwj6x h780), Скидка (b7nkaihbjgemqliwj6z h780). Инфинити-скролл (Group-строки) + анимации мем/золотая гора — после.
- Ошибка 3 (фигуры) — на скринах Услуг/Направлений фигуры рендерятся из Builder.io нормально, плейсхолдеров нет. Считаю auto-fixed; финально подтвердить общим скрином.

## Лог (ночь ~03:40)
- ✅ Собран блок **Кейсы** (keysy apeb02a2lkdmqliwj6m, h1024) — «КЕЙСЫ МАРКЕТИНГОВОГО ПРОДВИЖЕНИЯ» + ЗОЛОТАЯ ГОРА (3D, золотая) с метками клиентов 1-5 («наведи мышкой»). Это и есть «золотая гора» из задания! Рендерится 1:1, Coolvetica. Вставлен после Направлений.
- Анимация золотой горы (hover на метки → показать кейс) — best-effort ПОЗЖЕ (Builder.io HTML статичный; метки 1-5 на месте).
- ДАЛЕЕ: Создание (s7nsthpckxmqliwj6s) → Q&A (7bban6jd39pmqliwj6u) → Процесс (woxje2u098fmqliwj6x h780) → Скидка (b7nkaihbjgemqliwj6z h780). Потом инфинити-скролл Group-строки + анимации hover.

## Лог (ночь ~04:10)
- ✅ Собран блок **Создание** (sozdanie s7nsthpckxmqliwj6s, h1024) — «СОЗДАНИЕ САЙТОВ И ПРИЛОЖЕНИЙ ПОД МАРКЕТИНГ», 3 карточки (Сайты под ключ/Лендинги/Мобильные приложения) + лента клиентских кейсов (HAPPY BABY, TEA-REX, TAILPLACE, ESKO BAR, КЕТОROL) с «наведи мышкой чтобы остановить» = инфинити-скролл. Coolvetica. Вставлен после Кейсов.
- Маркетинг собран: header→Направления→Кейсы→Создание. ДАЛЕЕ: Q&A (7bban6jd39pmqliwj6u) → Процесс (woxje2u098fmqliwj6x h780) → Скидка (b7nkaihbjgemqliwj6z h780). Потом анимировать ленты (.marquee-track) в Создании/Кейсах.

## Лог (ночь ~04:42)
- ✅ Собран блок **Q&A** (qa 7bban6jd39pmqliwj6u, h1024) — «ЧАСТЫЕ ВОПРОСЫ ПРО МАРКЕТИНГ», аккордеон вопросов. Slug латиницей `qa`. Вставлен после Создания. Проверено скрином на iamagency-site.vercel.app (y≈9291): блок рендерится 1:1 между Созданием и Отзывами. Деплой iamagency-site-aieh62k6o ready.
- Маркетинг: header→Направления→Кейсы→Создание→Q&A. ДАЛЕЕ: Процесс (woxje2u098fmqliwj6x h780) → Скидка (b7nkaihbjgemqliwj6z h780, обратный отсчёт). Потом анимации (ленты .marquee-track + hover).

## Лог (ночь ~05:12)
- ⚙️ ФИКС высоты: build_block.py раньше жёстко вшивал `height:1024px` в корень любого блока → у блоков <1024 был бы белый провал снизу. Теперь скрипт читает РЕАЛЬНУЮ высоту из исходного root-стиля и экспортирует `export const <slug>H = <H>`. Перегенерил все: подтвердилось — все блоки 1024, кроме **Процесс=780** (Скидка тоже будет 780). napravleniya/qa не докачались (сеть 10060) — старые gen целы, они 1024, h не нужен.
- ✅ Собран блок **Процесс** (process woxje2u098fmqliwj6x, h=780) — «ПРОЦЕСС», шаги Брифинг/Стратегия/Запуск/Аналитика + «Маркетинг под ключ». Вставлен `<BuilderBlock html={processHtml} h={processH}/>` после Q&A. Проверено скрином (y≈10315): стоит между Q&A и Отзывами, без провала. Деплой iamagency-site-8g9un4h1l ready.
- Маркетинг: header→Направления→Кейсы→Создание→Q&A→Процесс. ДАЛЕЕ ПОСЛЕДНИЙ кусок: Скидка (b7nkaihbjgemqliwj6z h≈780, обратный отсчёт). Потом анимации (ленты + hover мем/гора).

## Лог (ночь ~05:38)
- ✅ Собран блок **Скидка** (skidka b7nkaihbjgemqliwj6z, h=780) — «СКИДКА НА ПРОДВИЖЕНИЕ / ДО КОНЦА МЕСЯЦА», промокод «а у вас скидки есть?» → -10%, кнопка «Получить скидку», счётчик ДНЕЙ/ЧАСОВ/МИНУТ. Вставлен `<BuilderBlock html={skidkaHtml} h={skidkaH}/>` после Процесса, перед Отзывами. Проверено скрином (y≈11095): блок на месте между Процессом и Отзывами. Деплой iamagency-site-b73ma7d9o ready (со 2й попытки — 1я socket hang up).
- 🎉 **МАРКЕТИНГ СОБРАН ЦЕЛИКОМ**: header→Направления→Кейсы→Создание→Q&A→Процесс→Скидка.
- ОСТАЛИСЬ АНИМАЦИИ (best-effort, по одной за итерацию): (a) живой обратный отсчёт в Скидке (сейчас «00 00 00» статика — нужен JS-таймер); (b) бесконечные ленты в Создании/Кейсах (.marquee-track); (c) появление мема в Направлениях; (d) hover на метки 1-5 золотой горы в Кейсах.

## Лог (ночь ~06:01)
- ✅ АНИМАЦИЯ (a) ЖИВОЙ ОТСЧЁТ в Скидке РАБОТАЕТ. Создан клиентский компонент `src/app/blocks/SkidkaCountdown.tsx` — обёртка поверх BuilderBlock, после маунта находит 4 ячейки `[layer-name="00"]` (дни/часы/минуты/секунды) и обновляет span раз в секунду до начала следующего месяца (цель считается на клиенте, без Date.now() на сервере). В page.tsx skidka теперь через `<SkidkaCountdown html={skidkaHtml} h={skidkaH}/>`. ПРОВЕРЕНО двумя eval с паузой 2.5с: `10|17|58|28` → `10|17|58|25` (секунды убывают), скрин показывает `10 17 58 10`. Деплой iamagency-site-8ybbgvnux ready.
- ДАЛЕЕ анимации: (b) бесконечные ленты в Создании/Кейсах → (c) мем в Направлениях → (d) hover золотой горы.

## Лог (ночь ~06:30)
- ✅ АНИМАЦИЯ (b) БЕСКОНЕЧНАЯ ЛЕНТА в Создании РАБОТАЕТ. Создан клиентский компонент `src/app/blocks/MarqueeBlock.tsx` — обёртка поверх BuilderBlock: после маунта глубоким запросом находит карточки ряда по сигнатуре (top:11px/height:174px), берёт их общего родителя (вложенная обёртка 1310×628), собирает в «трек» (div 0×0), клонирует на ширину периода и крутит через Web Animations API (45px/с, пауза при ховере). Абсолютные координаты Figma сохранены, уезжающие клоны клипает overflow холста. В page.tsx Создание теперь `<MarqueeBlock html={sozdanieHtml} rowTop={11} rowHeight={174}/>`. ГРАБЛЯ: сначала искал карточки в прямых детях 1440-холста — они оказались во вложенной обёртке (deepMatch). ПРОВЕРЕНО двумя eval transform: −127px → −207px за 1.5с (≈53px/с, движется), в треке 6 карточек (3+3 клона), скрин — вёрстка цела. Деплой iamagency-site-p2ztil0lx ready.
- ⚠️ Кейсы — ряда карточек в текущем keysyHtml НЕТ (инфинити-карточки лежат в ассете bnw1qrytneemqliwj6p, который не входит в собранный keysy-блок). Лента к Кейсам не применима без досборки этого ассета — отдельная задача, не блокирует.
- ДАЛЕЕ анимации: (c) появление мема в Направлениях → (d) hover золотой горы.

## Лог (ночь ~06:55)
- ✅ АНИМАЦИЯ (c) ПОЯВЛЕНИЕ МЕМА в Направлениях РАБОТАЕТ. Мем = бейдж `layer-name="Класс"` + эмодзи 🙌🏻/🤵🏼‍♂️. Создан клиентский компонент `src/app/blocks/AppearBlock.tsx` — обёртка поверх BuilderBlock: при въезде блока во вьюпорт (IntersectionObserver threshold .25) перечисленные по layer-name + ВСЕ эмодзи (детект `\p{Extended_Pictographic}`, кружки «+» не цепляет) всплывают opacity 0→1 + scale .8→1 с bounce и stagger 130мс. В page.tsx Направления теперь `<AppearBlock html={napravleniyaHtml} targets={["Класс"]}/>`. ПРОВЕРЕНО eval: до скролла 4 мем-элемента opacity=0, после scroll к блоку — opacity=1 (остальные эмодзи в др. блоках не тронуты, т.к. обёртка скоупится на свой root). Деплой iamagency-site-mfdzomvfn ready.
- ДАЛЕЕ ПОСЛЕДНЯЯ анимация: (d) hover на метки 1-5 золотой горы в Кейсах → показать кейс.

## Лог (ночь ~07:19) — ФИНАЛ
- ✅ АНИМАЦИЯ (d) HOVER МЕТОК золотой горы в Кейсах РАБОТАЕТ. Метки = `layer-name "1".."5"` (цифры в кружках). Скрытых карточек кейсов в экспорте НЕТ → сделан интерактив самих меток. Создан клиентский компонент `src/app/blocks/HoverMarkers.tsx` — обёртка поверх BuilderBlock: на кружок-родитель каждой цифры вешает mouseenter/mouseleave → scale(1.4) + золотое свечение drop-shadow(255,184,0) + brightness + cursor pointer (только transform/filter/z-index, координаты не трогаются). В page.tsx Кейсы теперь `<HoverMarkers html={keysyHtml} labels={["1".."5"]}/>`. ПРОВЕРЕНО eval (dispatchEvent mouseenter): targets=5, transform none→scale(1.4), filter→золотое свечение, mouseleave→реверт. Деплой iamagency-site-e0pe4ul1f ready.
- 🎉🎉 ВСЕ 4 АНИМАЦИИ ГОТОВЫ: (a) живой отсчёт Скидки ✅, (b) бесконечная лента Создания ✅, (c) появление мема в Направлениях ✅, (d) hover меток золотой горы ✅.
- ⚠️ Незакрытое (не блокирует, на день): лента в Кейсах (инфинити-карточки лежат в ассете bnw1qrytneemqliwj6p, не входит в собранный keysy — нужна досборка); карточки кейсов при клике на метку (в экспорте их нет — нужен доп. фрейм из Figma).
- ИТОГ НОЧИ: homepage iamagency собран целиком из Builder.io (1:1), Маркетинг достроен (7 блоков), 5 ошибок закрыты, Coolvetica, 4 анимации. Прод: https://iamagency-site.vercel.app . Домен iamagency.su (Тильда) не трогали.
